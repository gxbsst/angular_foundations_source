describe('Todo', function(){
   //var todoService = null;
   //var newTodo  = {
   //   name: 'test name'
   //};
   //var httpBackend;
   //beforeEach(module('app'));
   //beforeEach(inject(function(Todo, $httpBackend){
   //   httpBackend = $httpBackend;
   //   todoService = Todo;
   //   httpBackend.whenPOST('/todos', {
   //      todo: { title: 'title' }
   //   }).respond({id: 1});
   //}));
   //
   //it('can create a todo', function(){
   //   var result = null;
   //   httpBackend.expectPOST("/todos").respond({id: 1});
   //
   //   ctrl.post({todo: {title: 'title'}})
   //
   //   httpBackend.flush();
   //
   //});
});